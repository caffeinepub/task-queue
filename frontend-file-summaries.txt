{
  "frontend/index.html": "HTML entry point with browser tab title, favicon from generated assets, and DM Sans Google Font — CDN JS/CSS libraries removed to fix build errors.",
  "frontend/src/index.css": "Complete design system with OKLCH color tokens, DM Sans font, animation keyframes (slideUp, scaleIn, cardReveal), and utility classes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with DM Sans font, custom navy/sky/surface OKLCH color tokens, enhanced border radii, and card/modal/nav shadow utilities.",
  "frontend/src/utils/storage.ts": "Extended localStorage utilities adding a `CategoryEntry` interface (name + icon), `DEFAULT_CATEGORIES` array with emoji icons for Work/Personal/Education/Household/Shopping/Health/Other, and new `getCategoryEntries`, `deleteCategory` functions — with backward compatibility for old string-array category format.",
  "frontend/src/utils/auth.ts": "Authentication logic for login (with unverified account check returning `errorType: 'unverified'`) and signup (generates a 6-digit verification code, stores it with `verified: false`, returns code and email for modal display), plus logout helper.",
  "frontend/src/utils/avatar.ts": "Avatar utility mapping first letter of a user's name to a dynamic background color (Gmail-style) and extracting the uppercase initial.",
  "frontend/src/utils/notifications.ts": "Notification utility using sonner toast (instead of CDN Toastify) for success/info/warning messages.",
  "frontend/src/utils/validation.ts": "Validation helpers for email/phone format checking and a text highlight function that wraps search matches in a styled mark element.",
  "frontend/src/components/CustomPopup.tsx": "Reusable styled popup component for error, info, and confirm dialogs with fade+scale animation, keyboard (Escape) support, and no browser alert() usage.",
  "frontend/src/pages/Login.tsx": "Login page updated to display the same logo image (`/assets/generated/task-queue-logo.dim_256x256.png`) as DashboardHeader, with a graceful SVG fallback if the image fails to load.",
  "frontend/src/pages/Signup.tsx": "Signup page updated with the same logo image as DashboardHeader, plus real-time password criteria hint below the Password field — shows character count in red when under 6 characters, green checkmark when valid, and blocks form submission with a popup if the password is too short.",
  "frontend/src/components/TaskModal.tsx": "Add/Edit task modal with all six fields (title, description, category, status, due date, priority), fade+scale animation, pre-fill in edit mode, and sonner toast on save.",
  "frontend/src/components/TaskCard.tsx": "Individual task card displaying all task fields with colored category/status/priority badges, edit/delete action buttons, staggered fade-in animation, and hover lift effect.",
  "frontend/src/components/TaskList.tsx": "Task list grid rendering TaskCard components with staggered animations, or contextual empty-state messages when no tasks match the current filters.",
  "frontend/src/components/DashboardHeader.tsx": "Sticky dashboard header with logo image, search bar with clear button, Gmail-style user avatar, and logout button — all using inline SVGs and no CDN dependencies.",
  "frontend/src/components/Sidebar.tsx": "Animated sidebar with smart emoji suggestion on category input (50+ keyword→emoji mappings), delete (×) button visible only on user-created categories (hidden on hover), emoji icon display for all categories, and updated props to accept `userId`, `onDeleteCategory`, and `onAddCategory` with icon parameter.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page updated to pass `userId` and `onDeleteCategory` to Sidebar, handle category deletion (with active-category reset to All Tasks), and pass icon to `addCategory` calls.",
  "frontend/src/App.tsx": "Root app component managing view state (login/signup/dashboard) with session persistence check on mount, using sonner Toaster and no CDN dependencies.",
  "frontend/src/components/VerificationModal.tsx": "Email verification modal that displays the user's email and a simulated on-screen 6-digit code, accepts numeric input, validates it against the stored code, marks the user as verified in localStorage on success, and triggers a toast + navigation callback. Fixed conflicting `text-sm`/`text-lg` lint error and updated deprecated Tailwind v2 class names (`flex-shrink-0` → `shrink-0`, `placeholder-gray-400` → `placeholder:text-gray-400`)."
}